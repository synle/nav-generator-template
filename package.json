{
  "name": "nav-generator-template",
  "version": "1.2.0",
  "private": true,
  "description": "Nav Generator Template",
  "keywords": [
    "Le",
    "Sy"
  ],
  "license": "ISC",
  "author": "Sy Le",
  "scripts": {
    "clean": "rm -f index.html import-map.json .oxfmt-cache",
    "build": "bash build.sh",
    "format": "oxfmt --write .",
    "dev": "bash dev.sh",
    "start": "npx http-server --cors -a 127.0.0.1 .",
    "format:check": "oxfmt --check ."
  },
  "devDependencies": {
    "oxfmt": "^0.61.0"
  }
}
